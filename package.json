{
  "name": "rudyisme-11ty",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:eleventy": "npx @11ty/eleventy",
    "watch:eleventy": "npx @11ty/eleventy --serve",
    "build:sass": "npx sass --no-source-map --style=compressed src/sass:public/assets/Css",
    "watch:sass": "npx sass src/sass:public/assets/Css --watch",
    "build": "npm run build:eleventy & npm run build:sass",
    "watch": "npm run watch:eleventy & npm run watch:sass"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.5.0",
    "sass": "^1.89.2"
  }
}
